add_namespace = american_road

########################################################################
# american_road.1 - Truck Attack in New Orleans
#
# Scheduled from common/on_actions at game start, with no delay (fires at start).
# No daily check runs. Any trigger below is an extra prerequisite,
# tested when the delay elapses.
########################################################################
country_event = {
	id = american_road.1
	title = american_road.1.t
	desc = american_road.1.d
	picture = GFX_report_event_new_project_14_

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = american_road.1.a

		custom_effect_tooltip = american_road.1.a_tt
		set_temp_variable = { chaos_delta = 1 }
		USA_chaos_add = yes
		add_stability = -0.01
		add_manpower = -15
	}
}

########################################################################
# american_road.2 - The Munich Security Conference Speech
#
# Scheduled from common/on_actions at game start, with a 44-day delay.
# No daily check runs. Any trigger below is an extra prerequisite,
# tested when the delay elapses.
########################################################################
country_event = {
	id = american_road.2
	title = american_road.2.t
	desc = american_road.2.d
	picture = GFX_report_event_new_project_15_

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = american_road.2.a

		custom_effect_tooltip = american_road.2.a_tt
		set_temp_variable = { apr_delta = 1 }
		USA_apr_add = yes
		set_temp_variable = { chaos_delta = 2 }
		USA_chaos_add = yes
	}

	option = {
		name = american_road.2.b

		custom_effect_tooltip = american_road.2.b_tt
		set_temp_variable = { apr_delta = -5 }
		USA_apr_add = yes
		set_temp_variable = { chaos_delta = -5 }
		USA_chaos_add = yes
	}
}

########################################################################
# american_road.3 - Elon's DOGE cuts over 6,000 jobs at IRS
#
# Scheduled from common/on_actions at game start, with a 50-day delay.
# No daily check runs. Any trigger below is an extra prerequisite,
# tested when the delay elapses.
########################################################################
country_event = {
	id = american_road.3
	title = american_road.3.t
	desc = american_road.3.d
	picture = GFX_report_event_new_project

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = american_road.3.a

		custom_effect_tooltip = american_road.3.a_tt
		set_temp_variable = { apr_delta = 1 }
		USA_apr_add = yes
		set_temp_variable = { chaos_delta = 2 }
		USA_chaos_add = yes
	}

	option = {
		name = american_road.3.b

		custom_effect_tooltip = american_road.3.b_tt
		set_temp_variable = { apr_delta = -3 }
		USA_apr_add = yes
		set_temp_variable = { chaos_delta = -2 }
		USA_chaos_add = yes
	}
}

########################################################################
# american_road.4 - New Russian Ambassador
#
# Scheduled from common/on_actions at game start, with a 58-day delay.
# No daily check runs. Any trigger below is an extra prerequisite,
# tested when the delay elapses.
########################################################################
country_event = {
	id = american_road.4
	title = american_road.4.t
	desc = american_road.4.d
	picture = GFX_report_event_untitled-1

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = american_road.4.a

		custom_effect_tooltip = american_road.4.a_tt
		set_temp_variable = { apr_delta = 1 }
		USA_apr_add = yes
	}
}

########################################################################
# american_road.5 - Please, come to Greenland!
#
# Scheduled from common/on_actions at game start, with a 71-day delay.
# No daily check runs. Any trigger below is an extra prerequisite,
# tested when the delay elapses.
########################################################################
country_event = {
	id = american_road.5
	title = american_road.5.t
	desc = american_road.5.d
	picture = GFX_report_event_untitled

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = american_road.5.a

		custom_effect_tooltip = american_road.5.a_tt
		set_temp_variable = { apr_delta = -7 }
		USA_apr_add = yes
		set_temp_variable = { chaos_delta = 6 }
		USA_chaos_add = yes
	}
}

########################################################################
# american_road.6 - Hello? Is this Putin?
#
# Scheduled from common/on_actions at game start, with a 76-day delay.
# No daily check runs. Any trigger below is an extra prerequisite,
# tested when the delay elapses.
########################################################################
country_event = {
	id = american_road.6
	title = american_road.6.t
	desc = american_road.6.d
	picture = GFX_report_event_putiom

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = american_road.6.a

		custom_effect_tooltip = american_road.6.a_tt
		set_temp_variable = { apr_delta = 3 }
		USA_apr_add = yes
		set_temp_variable = { chaos_delta = 1 }
		USA_chaos_add = yes
	}
}

########################################################################
# american_road.7 - Bodies of missing aid workers found in Gaza ‘mass grave’ following Israeli attacks
#
# Scheduled from common/on_actions at game start, with a 89-day delay.
# No daily check runs. Any trigger below is an extra prerequisite,
# tested when the delay elapses.
########################################################################
country_event = {
	id = american_road.7
	title = american_road.7.t
	desc = american_road.7.d
	picture = GFX_report_event_new_project_2_

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = american_road.7.a

		custom_effect_tooltip = american_road.7.a_tt
		set_temp_variable = { apr_delta = -3 }
		USA_apr_add = yes
		set_temp_variable = { chaos_delta = 5 }
		USA_chaos_add = yes
	}

	option = {
		name = american_road.7.b

		custom_effect_tooltip = american_road.7.b_tt
		set_temp_variable = { apr_delta = -5 }
		USA_apr_add = yes
		set_temp_variable = { chaos_delta = 3 }
		USA_chaos_add = yes
	}
}

########################################################################
# american_road.8 - More than 100 immigrants detained at an illegal after-hours nightclub in Colorado
#
# Scheduled from common/on_actions at game start, with a 116-day delay.
# No daily check runs. Any trigger below is an extra prerequisite,
# tested when the delay elapses.
########################################################################
country_event = {
	id = american_road.8
	title = american_road.8.t
	desc = american_road.8.d
	picture = GFX_report_event_new_project_33_

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = american_road.8.a

		custom_effect_tooltip = american_road.8.a_tt
		set_temp_variable = { apr_delta = 4 }
		USA_apr_add = yes
		set_temp_variable = { chaos_delta = 4 }
		USA_chaos_add = yes
	}
}

########################################################################
# american_road.9 - Tarrif, Tarrif, Tarrif...
#
# Scheduled from common/on_actions at game start, with a 152-day delay.
# No daily check runs. Any trigger below is an extra prerequisite,
# tested when the delay elapses.
########################################################################
country_event = {
	id = american_road.9
	title = american_road.9.t
	desc = american_road.9.d
	picture = GFX_report_event_new_project_4_

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = american_road.9.a

		custom_effect_tooltip = american_road.9.a_tt
		set_temp_variable = { apr_delta = -6 }
		USA_apr_add = yes
		set_temp_variable = { chaos_delta = 7 }
		USA_chaos_add = yes
	}
}

########################################################################
# american_road.100 - Death of Pope Francis
#
# Scheduled from common/on_actions at game start, with a 110-day delay.
# No daily check runs. Any trigger below is an extra prerequisite,
# tested when the delay elapses.
########################################################################
news_event = {
	id = american_road.100
	title = american_road.100.t
	desc = american_road.100.d
	picture = GFX_news_event_new_project_3_

	is_triggered_only = yes
	major = yes

	option = {
		name = american_road.100.a
	}
}
