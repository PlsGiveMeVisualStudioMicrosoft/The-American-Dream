simp_propoganda_category = {

	simp_choose_poster_1 = {
		icon = eng_ally_imperialist_coup

		days_remove = 7
		allowed = { original_tag = USA }
		available = { has_global_flag = usa_trump_inaugurated }
		visible = { always = yes }

		cost = 25

		# was: add_to_variable = { aproval_rate = 1 }
		#
		# Writing to aproval_rate directly no longer works properly -
		# it is a rounded display variable and the next monthly tick
		# overwrites it from usa_apr_real. Use the helper instead:
		# it moves the real value, refreshes the bar, and re-checks
		# the tier spirit immediately.
		complete_effect = {
			set_temp_variable = { apr_delta = 25 }
			USA_apr_add = yes
		}
	}

	simp_choose_poster_2 = {
		icon = eng_ally_imperialist_coup

		days_remove = 7
		allowed = { original_tag = USA }
		available = { has_global_flag = usa_trump_inaugurated }
		visible = { always = yes }

		cost = 25

		# was: add_to_variable = { aproval_rate = 1 }
		#
		# Writing to aproval_rate directly no longer works properly -
		# it is a rounded display variable and the next monthly tick
		# overwrites it from usa_apr_real. Use the helper instead:
		# it moves the real value, refreshes the bar, and re-checks
		# the tier spirit immediately.
		complete_effect = {
			set_temp_variable = { apr_delta = -25 }
			USA_apr_add = yes
		}
	}

}
