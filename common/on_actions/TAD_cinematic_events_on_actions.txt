on_actions = {

    on_daily_HAI = {
        effect = {

# One-time migration for saves that changed between cinematic and AI
# overhaul builds. Old suppression flags previously prevented the
# painted World News events from firing again.
if = {
    limit = { NOT = { has_global_flag = TAD_cine_runtime_fix_v4 } }
    set_global_flag = TAD_cine_runtime_fix_v4
    clr_global_flag = TAD_cine_haiti_fallen
    clr_global_flag = TAD_cine_haiti_restored
    clr_global_flag = TAD_cine_belize_fallen
    clr_global_flag = TAD_cine_belize_survived
    clr_global_flag = TAD_cine_guatemala_victory
    clr_global_flag = TAD_cine_guatemala_defeat
    HAI = {
        clr_country_flag = TAD_cine_port_au_prince_fallen
        clr_country_flag = TAD_cine_capital_reclaimed
    }
    BLZ = {
        clr_country_flag = TAD_cine_belmopan_fallen
    }
    GUA = {
        clr_country_flag = TAD_cine_captured_belmopan
        clr_country_flag = TAD_cine_crossed_border
    }
}
            # The capital falls and later returns.
            if = {
                limit = {
                    tag = HAI
                    NOT = { controls_state = 74 }
                    NOT = { has_country_flag = TAD_cine_port_au_prince_fallen }
                }
                set_country_flag = TAD_cine_port_au_prince_fallen
                country_event = { id = tad_cine.1 hours = 3 }
                news_event = { id = tad_cine.101 hours = 8 }
            }
            if = {
                limit = {
                    tag = HAI
                    controls_state = 74
                    has_country_flag = TAD_cine_port_au_prince_fallen
                    NOT = { has_country_flag = TAD_cine_capital_reclaimed }
                }
                set_country_flag = TAD_cine_capital_reclaimed
                country_event = { id = tad_cine.10 hours = 3 }
            }

            # Regional losses receive their own visual reports.
            if = {
                limit = {
                    tag = HAI
                    NOT = { controls_state = 83 }
                    NOT = { has_country_flag = TAD_cine_cap_haitien_fallen }
                }
                set_country_flag = TAD_cine_cap_haitien_fallen
                country_event = { id = tad_cine.8 hours = 4 }
            }
            if = {
                limit = {
                    tag = HAI
                    NOT = { controls_state = 87 }
                    NOT = { has_country_flag = TAD_cine_gonave_plundered }
                }
                set_country_flag = TAD_cine_gonave_plundered
                country_event = { id = tad_cine.9 hours = 4 }
            }

            # One-time major gang actions are staggered after the crisis worsens.
            if = {
                limit = {
                    tag = HAI
                    has_country_flag = HAI_crisis_active
                    check_variable = { HAI_escalation > 39 }
                    NOT = { has_country_flag = TAD_cine_bank_raid_scheduled }
                }
                set_country_flag = TAD_cine_bank_raid_scheduled
                country_event = { id = tad_cine.3 days = 7 random_days = 24 }
            }
            if = {
                limit = {
                    tag = HAI
                    has_country_flag = HAI_crisis_active
                    check_variable = { HAI_escalation > 47 }
                    NOT = { has_country_flag = TAD_cine_convoy_raid_scheduled }
                }
                set_country_flag = TAD_cine_convoy_raid_scheduled
                country_event = { id = tad_cine.4 days = 12 random_days = 30 }
            }
            if = {
                limit = {
                    tag = HAI
                    has_country_flag = HAI_crisis_active
                    check_variable = { HAI_escalation > 54 }
                    NOT = { has_country_flag = TAD_cine_prison_break_scheduled }
                }
                set_country_flag = TAD_cine_prison_break_scheduled
                country_event = { id = tad_cine.6 days = 16 random_days = 36 }
            }
            if = {
                limit = {
                    tag = HAI
                    has_country_flag = HAI_crisis_active
                    check_variable = { HAI_escalation > 59 }
                    NOT = { has_country_flag = TAD_cine_palace_loot_scheduled }
                }
                set_country_flag = TAD_cine_palace_loot_scheduled
                country_event = { id = tad_cine.2 days = 18 random_days = 40 }
            }
            if = {
                limit = {
                    tag = HAI
                    has_country_flag = HAI_crisis_active
                    check_variable = { HAI_escalation > 64 }
                    NOT = { has_country_flag = TAD_cine_port_fire_scheduled }
                }
                set_country_flag = TAD_cine_port_fire_scheduled
                country_event = { id = tad_cine.5 days = 20 random_days = 42 }
            }
            if = {
                limit = {
                    tag = HAI
                    has_country_flag = HAI_crisis_active
                    check_variable = { HAI_escalation > 69 }
                    NOT = { has_country_flag = TAD_cine_airport_silent_scheduled }
                }
                set_country_flag = TAD_cine_airport_silent_scheduled
                country_event = { id = tad_cine.7 days = 22 random_days = 48 }
            }
            if = {
                limit = {
                    tag = HAI
                    has_country_flag = HAI_crisis_active
                    check_variable = { HAI_escalation > 44 }
                    NOT = { has_country_flag = TAD_cine_corridor_scheduled }
                }
                set_country_flag = TAD_cine_corridor_scheduled
                country_event = { id = tad_cine.11 days = 25 random_days = 50 }
            }

            # Gang capitulations. These checks also work after annexation.
            if = {
                limit = {
                    tag = HAI
                    OR = { NOT = { country_exists = VIV } VIV = { has_capitulated = yes } }
                    NOT = { has_global_flag = TAD_cine_viv_destroyed }
                }
                set_global_flag = TAD_cine_viv_destroyed
                country_event = { id = tad_cine.12 hours = 3 }
            }
            if = {
                limit = {
                    tag = HAI
                    OR = { NOT = { country_exists = GRF } GRF = { has_capitulated = yes } }
                    NOT = { has_global_flag = TAD_cine_grf_destroyed }
                }
                set_global_flag = TAD_cine_grf_destroyed
                country_event = { id = tad_cine.13 hours = 3 }
            }
            if = {
                limit = {
                    tag = HAI
                    OR = { NOT = { country_exists = KRB } KRB = { has_capitulated = yes } }
                    NOT = { has_global_flag = TAD_cine_krb_destroyed }
                }
                set_global_flag = TAD_cine_krb_destroyed
                country_event = { id = tad_cine.14 hours = 3 }
            }

            # National collapse and final restoration.
            if = {
                limit = {
                    tag = HAI
                    has_capitulated = yes
                    NOT = { has_global_flag = TAD_cine_haiti_fallen }
                }
                set_global_flag = TAD_cine_haiti_fallen
                news_event = { id = tad_cine.102 hours = 6 }
            }
            if = {
                limit = {
                    tag = HAI
                    NOT = { has_capitulated = yes }
                    has_country_flag = HAI_haiti_war_started
                    NOT = { has_country_flag = HAI_crisis_active }
                    OR = { NOT = { country_exists = VIV } VIV = { has_capitulated = yes } }
                    OR = { NOT = { country_exists = GRF } GRF = { has_capitulated = yes } }
                    OR = { NOT = { country_exists = KRB } KRB = { has_capitulated = yes } }
                    NOT = { has_global_flag = TAD_cine_haiti_restored }
                }
                set_global_flag = TAD_cine_haiti_restored
                country_event = { id = tad_cine.15 hours = 4 }
                news_event = { id = tad_cine.106 hours = 10 }
            }
        }
    }

    on_daily_BLZ = {
        effect = {

if = {
    limit = {
        tag = BLZ
        has_completed_focus = BLZ_prepare_armed_personnel
        NOT = { has_country_flag = TAD_cine_armed_personnel_ready }
    }
    set_country_flag = TAD_cine_armed_personnel_ready
    country_event = { id = tad_cine.16 hours = 3 }
}
if = {
    limit = {
        tag = BLZ
        has_completed_focus = BLZ_start_drills_launch_intelligence
        NOT = { has_country_flag = TAD_cine_drills_reported }
    }
    set_country_flag = TAD_cine_drills_reported
    country_event = { id = tad_cine.17 hours = 3 }
    GUA = { country_event = { id = tad_cine.18 days = 1 } }
}
if = {
    limit = {
        tag = BLZ
        has_completed_focus = BLZ_solicit_protection
        NOT = { has_country_flag = TAD_cine_protection_requested }
    }
    set_country_flag = TAD_cine_protection_requested
    country_event = { id = tad_cine.19 hours = 4 }
}
if = {
    limit = {
        tag = BLZ
        check_variable = { BLZ_GUA_tension > 49 }
        NOT = { has_war_with = GUA }
        has_completed_focus = BLZ_start_drills_launch_intelligence
        GUA = { has_completed_focus = GUA_expand_peten_patrol_focus }
        NOT = { has_country_flag = TAD_cine_border_incident_cd }
    }
    set_country_flag = TAD_cine_border_incident_cd
    country_event = { id = tad_cine.25 days = 3 random_days = 9 }
    GUA = {
        set_country_flag = TAD_cine_border_incident_cd
        country_event = { id = tad_cine.26 days = 3 random_days = 9 }
    }
}
            if = {
                limit = {
                    tag = BLZ
                    has_completed_focus = BLZ_fortify_mopan_border
                    NOT = { has_country_flag = TAD_cine_forts_reported }
                }
                set_country_flag = TAD_cine_forts_reported
                country_event = { id = tad_cine.20 hours = 3 }
            }
            if = {
                limit = {
                    tag = BLZ
                    check_variable = { BLZ_foreign_contingents > 0 }
                    NOT = { has_country_flag = TAD_cine_volunteers_reported }
                }
                set_country_flag = TAD_cine_volunteers_reported
                country_event = { id = tad_cine.21 hours = 3 }
            }
            if = {
                limit = {
                    tag = BLZ
                    has_completed_focus = BLZ_expand_port_belize
                    NOT = { has_country_flag = TAD_cine_port_belize_reported }
                }
                set_country_flag = TAD_cine_port_belize_reported
                country_event = { id = tad_cine.22 hours = 3 }
            }
            if = {
                limit = {
                    tag = BLZ
                    has_war_with = GUA
                    NOT = { controls_state = 88 }
                    NOT = { has_country_flag = TAD_cine_belmopan_fallen }
                }
                set_country_flag = TAD_cine_belmopan_fallen
                country_event = { id = tad_cine.23 hours = 3 }
            }
            if = {
                limit = {
                    tag = BLZ
                    has_capitulated = yes
                    NOT = { has_global_flag = TAD_cine_belize_fallen }
                }
                set_global_flag = TAD_cine_belize_fallen
                news_event = { id = tad_cine.107 hours = 6 }
            }
            if = {
                limit = {
                    tag = BLZ
                    has_country_flag = BLZ_GUA_war_started
                    NOT = { has_war_with = GUA }
                    NOT = { has_capitulated = yes }
                    NOT = { has_global_flag = TAD_cine_belize_survived }
                }
                set_global_flag = TAD_cine_belize_survived
                country_event = { id = tad_cine.24 hours = 4 }
                news_event = { id = tad_cine.108 hours = 10 }
            }
        }
    }

    on_daily_GUA = {
        effect = {
            if = {
                limit = {
                    tag = GUA
                    has_completed_focus = GUA_expand_peten_patrol_focus
                    NOT = { has_country_flag = TAD_cine_peten_mobilized }
                }
                set_country_flag = TAD_cine_peten_mobilized
                country_event = { id = tad_cine.30 hours = 3 }
            }
            if = {
                limit = {
                    tag = GUA
                    has_war_with = BLZ
                    NOT = { has_country_flag = TAD_cine_crossed_border }
                }
                set_country_flag = TAD_cine_crossed_border
                country_event = { id = tad_cine.31 hours = 3 }
            }
            if = {
                limit = {
                    tag = GUA
                    has_war_with = BLZ
                    controls_state = 88
                    NOT = { has_country_flag = TAD_cine_captured_belmopan }
                }
                set_country_flag = TAD_cine_captured_belmopan
                country_event = { id = tad_cine.32 hours = 3 }
                news_event = { id = tad_cine.109 hours = 9 }
            }
            if = {
                limit = {
                    tag = GUA
                    has_war_with = BLZ
                    BLZ = { check_variable = { BLZ_GUA_war_months > 3 } controls_state = 88 }
                    NOT = { has_country_flag = TAD_cine_jungle_stalemate }
                }
                set_country_flag = TAD_cine_jungle_stalemate
                country_event = { id = tad_cine.33 hours = 3 }
            }
            if = {
                limit = {
                    tag = GUA
                    has_war_with = BLZ
                    BLZ = { check_variable = { BLZ_GUA_war_months > 1 } }
                    OR = { controls_state = 15 controls_state = 88 }
                    NOT = { has_country_flag = TAD_cine_resistance_reported }
                }
                set_country_flag = TAD_cine_resistance_reported
                country_event = { id = tad_cine.34 hours = 3 }
            }
            if = {
                limit = {
                    tag = GUA
                    BLZ = { has_capitulated = yes }
                    NOT = { has_global_flag = TAD_cine_guatemala_victory }
                }
                set_global_flag = TAD_cine_guatemala_victory
                news_event = { id = tad_cine.110 hours = 8 }
            }

if = {
    limit = {
        tag = GUA
        has_war_with = BLZ
        BLZ = { check_variable = { BLZ_international_support > 69 } }
        BLZ = { NOT = { has_capitulated = yes } }
        OR = { NOT = { controls_state = 15 } NOT = { controls_state = 88 } }
        NOT = { has_country_flag = TAD_cine_pressure_mounts }
    }
    set_country_flag = TAD_cine_pressure_mounts
    country_event = { id = tad_cine.37 days = 2 random_days = 4 }
}
            if = {
                limit = {
                    tag = GUA
                    has_country_flag = TAD_cine_crossed_border
                    NOT = { has_war_with = BLZ }
                    BLZ = { NOT = { has_capitulated = yes } }
                    NOT = { has_global_flag = TAD_cine_guatemala_defeat }
                }
                set_global_flag = TAD_cine_guatemala_defeat
                country_event = { id = tad_cine.35 hours = 4 }
                news_event = { id = tad_cine.111 hours = 10 }
            }
        }
    }
}
