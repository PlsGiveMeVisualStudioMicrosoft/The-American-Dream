on_actions = {

	on_daily_USA = {
		effect = {
			USA = {

				# ---------- 2025.1.20 - Trump (frame 2) ----------
				if = {
					limit = {
						NOT = { has_global_flag = usa_trump_inaugurated }
						date > 2025.1.19
					}
					set_global_flag = usa_trump_inaugurated
					country_event = { id = usa_inauguration.2 hours = 23 }
					set_variable = { simp_slot_frame = 2 }
					log = "[GetDateText]: usa_inauguration driver 1 armed"

					# the two set_variable lines that used to sit
					# here now live inside USA_apr_arm_system, so
					# the starting numbers are tunable in one place
					USA_apr_arm_system = yes
				}

				# ---------- next president ----------
				# if = {
				# 	limit = {
				# 		NOT = { has_global_flag = usa_next_inaugurated }
				# 		date > 2029.1.19
				# 	}
				# 	set_global_flag = usa_next_inaugurated
				# 	set_variable = { simp_slot_frame = 4 }   # 4 = next president
				# }

			}
		}
	}

}
