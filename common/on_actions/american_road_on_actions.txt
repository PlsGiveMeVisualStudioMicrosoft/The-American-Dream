# ====================================================================
#  Precise-date drivers for the american_road events.
#
#  Every event is scheduled once, here at game start, with the
#  exact number of days from 2025.1.1 to its date. No daily
#  check runs and no guard flag is needed - the schedule fires it
#  a single time on the right day. Days are counted on the game's
#  own 365-day calendar, which has no leap day.
#
#  Any extra prerequisite lives in the event's own trigger block
#  and is checked when the delay elapses; if it fails then, the
#  event is simply skipped.
# ====================================================================

on_actions = {
	on_startup = {
		effect = {

			USA = {
				country_event = { id = american_road.1 }             # 2025.1.1 (at start)  Truch
			}
		}
	}
}
